local srl = require("serialization")
local settings = {}


function settings.getSettings(pathToFile, defaultSettings)
	
	return deSerSettings
end

function settings.setSettings(pathToFile, newSettings)

	return success
end

return settings
